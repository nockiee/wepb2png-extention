## WebP to PNG/JPG Downloader

### Firefox extension that automatically converts WebP images to PNG or JPG when downloading.
Installation
1. Go to ```about:debugging``` in Firefox
2. Click ```"This Firefox"``` → ```"Load Temporary Add-on"```
3. Select ```manifest.json```

Usage

1. Click extension icon to open settings
2. Toggle on/off with the switch
3. Choose ```PNG``` or ```JPG``` format
4. Download WebP images as usual - they auto-convert
